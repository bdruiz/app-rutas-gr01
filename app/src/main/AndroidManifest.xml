<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.apprutasmintic">
    <permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppMinticRutas">
        <activity
            android:name=".view.monitor.Monitor2"
            android:exported="true" />
        <activity
            android:name=".view.login.LoginActivity"
            android:theme="@style/Theme.AppMinticRutas.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.padres.Padres2" />
        <activity
            android:name=".view.padres.Padres1Activity"
            android:theme="@style/Theme.AppMinticRutas.NoActionBar"></activity>
        <activity
            android:name=".view.monitor.Monitor1"
            android:theme="@style/Theme.AppMinticRutas.NoActionBar"></activity>
        <activity android:name=".ActivityForgotPass" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>